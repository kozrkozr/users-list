import { EqualControlsContainerDirective } from './equal-controls-container.directive';

describe('EqualControlsContainerDirective', () => {
  it('should create an instance', () => {
    const directive = new EqualControlsContainerDirective();
    expect(directive).toBeTruthy();
  });
});
