import { ControlErrorRendererDirective } from './control-error-renderer.directive';

describe('ControlErrorRendererDirective', () => {
  it('should create an instance', () => {
    const directive = new ControlErrorRendererDirective();
    expect(directive).toBeTruthy();
  });
});
